## What & why

<!-- What does this change, and what problem does it solve? Link the issue. -->

Closes #

## What was rejected

<!-- Alternatives you considered and deliberately did not do. -->

## Checklist

- [ ] `npm run build` succeeds and the rebuilt `dist/` is committed
- [ ] `npm run typecheck` is clean
- [ ] `npm run lint` is clean
- [ ] `npm test` passes
- [ ] Docs updated if behavior changed (README, `docs/llms/rikiki-reference.md`)
- [ ] Visual change? Before/after screenshot attached
