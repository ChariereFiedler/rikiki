{
  "name": "site",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "node scripts/stage-assets.mjs && astro dev",
    "build": "node scripts/stage-assets.mjs && astro build && node scripts/check-dist.mjs",
    "preview": "astro preview",
    "stage": "node scripts/stage-assets.mjs",
    "check:dist": "node scripts/check-dist.mjs",
    "lint:dashes": "node scripts/lint-dashes.mjs",
    "lint:links": "node scripts/lint-links.mjs",
    "lint": "npm run lint:dashes && npm run lint:links && astro check"
  },
  "keywords": [],
  "author": "Cédric Chariere Fiedler",
  "license": "MIT",
  "engines": {
    "node": ">=22.12.0"
  },
  "type": "commonjs",
  "dependencies": {
    "astro": "^7.3.2"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "typescript": "^6.0.3"
  }
}
