{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "rootDir": "./src"
  },
  // tools/ is build-time code · it is type-checked, never published.
  "exclude": ["node_modules", "dist", "src/**/*.test.ts", "tools"]
}
