{
  "name": "rikiki-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Wrapper for the tordu-jardin static-site CI template · the real build lives in site/. See .gitlab-ci.yml for the BUILD_CMD override.",
  "scripts": {
    "build": "cd site && npm ci && npm run build"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
